/* =========================================================
   Versione app — un solo posto da aggiornare ad ogni push.
   Usata sia per mostrarla nell'interfaccia sia per invalidare
   la cache del service worker (vedi service-worker.js).
   ========================================================= */

const APP_VERSION = '2.1.0';
